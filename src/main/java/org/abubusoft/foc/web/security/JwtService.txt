package org.abubusoft.foc.web.security;

import org.abubusoft.foc.web.security.ng.JwtUser;

public interface JwtService {
	String generateToken(InterrogazioniUserIdentity user, long lastAccess, long maxInactiveInterval);

	JwtUser getUser(JwtAuthentication authentication);
	
	JwtAuthentication decodeToken(String authToken);

	JwtAuthentication createAnonymousUser();

}
